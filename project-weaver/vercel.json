{
  "version": 2,
  "buildCommand": "cd dashboard && npm run build",
  "outputDirectory": "dashboard/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dashboard/$1"
    }
  ]
}
