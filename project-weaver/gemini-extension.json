{
  "name": "project-weaver",
  "version": "1.0.0",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "weaver": {
      "command": "node",
      "args": ["${extensionPath}${/}dist${/}index.js"],
      "cwd": "${extensionPath}"
    }
  }
}
